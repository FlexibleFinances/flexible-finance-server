export enum templateTypeEnum {
  Account = "Account",
  Entity = "Entity",
  Transaction = "Transaction",
}

export enum fieldTypeTypeEnum {
  Text = "Text",
  Number = "Number",
  Date = "Date",
  Boolean = "Boolean",
}
